- if object != current_user
  - if current_user.friend?(object)
    = link_to 'Unfriend', friendship_path(object), method: :delete
  - else
    = link_to 'Friend', friendships_path(friend_id: object),method: :post