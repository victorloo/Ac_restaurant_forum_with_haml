- if object != current_user
  - if current_user.friend?(object)
    = link_to 'Unfriend', friendship_path(object), method: :delete, class: "btn btn-danger"
  - else
    = link_to 'Friend', friendships_path(friend_id: object),method: :post, class: "btn btn-default"