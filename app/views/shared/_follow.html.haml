- if object != current_user
  - if current_user.following?(object)
    = link_to 'Unfollow', followship_path(object), method: :delete, class: "btn btn-danger"
  - else
    = link_to 'Follow', followships_path(following_id: object), method: :post, class: "btn btn-default"